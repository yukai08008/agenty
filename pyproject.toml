[project]
name = "agenty"
version = "0.1.0"
description = "Agent-centered project development, management, and maintenance"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0.0",
]

[project.scripts]
agenty = "agenty.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
